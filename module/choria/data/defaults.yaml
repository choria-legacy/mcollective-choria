lookup_options:
  mcollective_choria::package_dependencies:
    merge:
      strategy: deep
  mcollective_choria::config:
    merge:
      strategy: deep
  mcollective_choria::client_config:
    merge:
      strategy: deep
  mcollective_choria::server_config:
    merge:
      strategy: deep
